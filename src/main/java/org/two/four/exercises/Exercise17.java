package org.two.four.exercises;

/**
 * 2.4.17 证明：构造大小为k的面向最小元素的优先队列，然后进行 N-k 次替换最小元素的操作后
 * （删除最小元素后再插入元素），N个元素中的前 k 大元素均会留在优先队列中。
 *
 * @author cheng
 *         2018/2/2 20:28
 */
public class Exercise17 {
    /*
     *
     * 因为 k 是一个优先队列，所以每次删除元素会删除最小元素；
     * 再因为 N-k 次替换，每次都是删除最小元素，所以最后留在
     * k 中的元素为 N 个 元素中前 k 大的。
     *
     */
}
